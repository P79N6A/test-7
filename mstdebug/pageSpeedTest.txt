http://mst.vip.com/384545.php
http://mst.vip.com/373735.php

图片
----
提前加载 
function loadEarly() {
    imgUrls = [].slice.call($('img[src*=http]').map((i, img) => img.src));
    copy(imgUrls);
    imgUrls.forEach(url => {
        if( url.indexOf('placeholder') == -1) {
            var im = new Image();
            im.src = url;
        }
    });
    
}


并发
cookie


缓存
----
expires
max-age

dns
-----

